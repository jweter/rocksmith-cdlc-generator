schema_version: 1
project: rocksmith-cdlc-generator
updated_at: "2026-08-21T13:52:00Z"
status_source: "Verified repository state plus PROJECT_PLAN.md; this file is a continuity cache and never overrides higher-authority evidence."

product:
  primary_platform: "Windows 11 desktop application"
  primary_surface: "GUI"
  deterministic_engine_surface: "CLI"
  target_game: "Rocksmith 2014 Remastered"
  arrangements:
    - bass
    - lead
    - rhythm
  arrangement_priority: "equal"
  north_star: "One local recording plus one complete structured score, when available, producing reviewed playable Bass + Lead + Rhythm arrangements with minimal manual authoring."

current_milestone:
  name: "Product Reality Gate v1"
  roadmap_file: "PROJECT_PLAN.md"
  state: "active"
  objective: "Run one representative lawful song + complete structured score through the packaged Windows application, capture measured workflow evidence, and use the findings to prioritize normal-path fixes."

roadmap_issue_queue:
  policy: "Every open product issue receives an explicit roadmap disposition so GitHub Issues remain actionable inputs to scheduled development rather than a disconnected backlog."
  ordered_items:
    - issue: 304
      disposition: "current milestone / highest priority"
      action: "Use as the umbrella Product Reality defect-remediation pass. Reproduce and fix normal-path blockers, stale/incorrect authority, validation/export/package defects, and high-value workflow failures; add regression protection and focused child issues where needed. Human-only packaged verification is a waiting lane, not a repo-wide freeze."
    - issue: 305
      disposition: "parallel product-progress lane / evidence-driven authoring hardening"
      action: "Alongside #304 remediation, incrementally improve the primary Windows authoring workflow where work is independent of pending human verification; preserve provenance/review/validation gates."
    - issue: 193
      disposition: "standing engineering practice / all milestones"
      action: "Cross-link recurring defect classes from Product Reality, CI, and UI work; record shared root cause, prevention rule, and regression coverage."
    - issue: 46
      disposition: "later capability expansion"
      action: "Optional pluggable Apple Music metadata enrichment for benchmark candidates only after core desktop workflow completion; never use it as an audio source or hard build/runtime dependency."

automation_contract:
  schedule_role: "repository participant in the consolidated hourly portfolio-development loop"
  branch_prefix: "agent/"
  new_roadmap_pr_budget_per_hour: 1
  query_live_github_state_every_run: true
  newly_opened_pr_requires_fresh_ci_before_merge: true
  required_pr_workflows:
    - name: "CI"
      file: ".github/workflows/ci.yml"
    - name: "Windows Desktop"
      file: ".github/workflows/windows-desktop.yml"
  merge_rule: "Merge only the exact inspected head SHA after both required workflows complete successfully, mergeability is resolved, and no blocking review or material correctness/safety/licensing/provenance/product concern remains."
  advisory_review_unavailable_is_not_failure: true

verified_repository_state:
  checked_at: "2026-08-21T13:52:00Z"
  note: "Live PR/check/issue state is intentionally not cached as authoritative beyond this snapshot. Scheduled runs must query GitHub directly before acting."
  recently_merged_pr:
    number: 332
    title: "Document EOF fret-hand-position investigation contract"
    merged_at: "2026-08-21T13:22:37Z"
    behavior_change: "The EOF integration lane now has an explicit evidence and safety contract for future fret-hand-position/global fretboard optimization work; no optimizer behavior is yet authorized."
    pending_verification: "Regenerate Bass mapping and validation in the same packaged Windows Product Reality project and report whether the prior 97 hard unmapped-note failures remain. No PASS/FAIL is inferred without that evidence."
  active_branch_work:
    branch: "agent/fretboard-candidate-inventory"
    lane: "product progress / independent of pending Bass retest"
    planned_behavior_change: "Enumerate the complete pitch-correct string/fret search space for one imported source track so ambiguous fingering can be measured before any optimizer or EOF-derived preference is implemented."
    eof_integration: "EOF remains advisory only; the candidate inventory is project-owned and read-only and does not import EOF edits or choose accepted fingering."
  ci_workflows:
    - ".github/workflows/ci.yml"
    - ".github/workflows/windows-desktop.yml"
  open_issue_count_at_check: 4
  important_open_issues:
    - number: 304
      title: "Audit and fix current Product Reality defects before expanding scope"
      relevance: "Current-milestone remediation umbrella; packaged Bass validation is waiting on human evidence while independent work continues."
    - number: 305
      title: "Upgrade the Windows desktop UI to a polished authoring workspace"
      relevance: "Parallel authoring-hardening lane; fretboard candidate analysis and EOF comparison work are independently justified and do not assume the Bass retest outcome."
    - number: 193
      title: "Track recurring defect patterns and root-cause fixes"
      relevance: "Standing engineering-memory anchor used across all milestones whenever a failure class recurs."
    - number: 46
      title: "Automate Apple Music metadata enrichment for benchmark candidates"
      relevance: "Explicit later capability expansion; optional external metadata integration that must not displace core desktop completion."

completed_foundation_summary:
  - "Immutable ingest and normalization"
  - "Beat/tempo analysis and Bass transcription/mapping"
  - "Guitar Pro 3-5 and MusicXML complete-score intake"
  - "Explicit Bass/Lead/Rhythm mapping and shared score-to-recording timing"
  - "Reviewed timing and provenance-aware arrangement editing"
  - "Rocksmith XML authoring, DLC Builder staging, and PSARC registration/verification"
  - "Packaged Windows desktop executable and Song Workspace"
  - "Transactional provenance-aware undo/redo across accepted review layers"
  - "Human-confirmed multiple source tracks per Rocksmith arrangement role with downstream review/preview support"
  - "Optional Editor on Fire external reference bridge for immutable registered GP3/GP4/GP5 scores"
  - "Deterministic lawful EOF compatibility fixture and source-bound importer comparison contract"
  - "Project-local provenance-bound EOF discrepancy reporting with importer/runtime invalidation"
  - "Documented EOF fret-hand-position investigation contract"

next_continuation:
  priority: "P4-current-milestone / parallel lanes"
  action: "Human validation lane: regenerate Bass mapping/validation in the packaged representative project and report residual failures. Product-progress lane: establish a read-only pitch-correct fretboard candidate inventory; after it is independently green, capture provenance-bound EOF fret-hand-position observations for an original ambiguous fixture before authorizing any optimization objective."
  selection_rule: "Before beginning roadmap work, query live PR state. Handle failed, green, blocked, conflicted, or pending existing work according to docs/agent-development-policy.md. A human-only Product Reality gate blocks only dependent work; continue the highest-value independent safe lane."

safety_and_content_boundaries:
  live_rocksmith_modification: false
  nocablelauncher_modification: false
  validation_gate_required_for_packaging: true
  commercial_audio_in_git: false
  private_psarc_cdlc_in_git: false
  copyrighted_personal_tab_score_in_git_without_explicit_redistribution_rights: false
  private_generated_project_workspaces_in_git: false
  eof_bundled_or_auto_downloaded: false
  eof_can_mutate_canonical_authority: false

human_review_gates:
  - "rights/provenance uncertainty"
  - "source mapping uncertainty"
  - "timing acceptance"
  - "fingering/playability"
  - "tone acceptance"
  - "package readiness"

human_decision_required:
  - issue: 304
    decision: "Regenerate Bass mapping and validation in the same packaged Windows representative project and report residual failures; this is a waiting human validation lane, not a repo-wide development blocker."
  - issue: 46
    decision: "Before implementation, confirm the product/licensing decision to add an external catalog metadata provider and the permitted metadata-cache boundary."

maintenance_contract:
  verify_every_scheduled_run: true
  update_when_project_reality_changes: true
  update_in_same_pr_as_relevant_change: true
  stale_status_is_a_defect: true
  timestamp_age_alone_requires_noop_pr: false
