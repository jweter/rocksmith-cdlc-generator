# EOF Subsystem Parity Matrix

Issue: #414  
Program: `docs/eof-reference-parity-program.md`

## How to use this matrix

This is the working comparison ledger between Rocksmith CDLC Generator and mature reference implementations, primarily Editor on Fire (EOF).

Status values:

- **UNASSESSED** — reference behavior not yet traced.
- **PARTIAL** — some behavior exists in both systems but parity is not established.
- **PARITY** — relevant behavior is intentionally equivalent and regression-protected.
- **PARITY+** — EOF-compatible baseline is preserved and this project deliberately extends it.
- **DIVERGENT** — behavior intentionally differs for a documented reason.
- **GAP** — mature behavior exists and this project is missing or materially wrong.
- **N/A** — not applicable to this project's product path.

Reuse values:

- **study** — use as design/behavior reference only.
- **port** — translate/adapt algorithm into this project's architecture.
- **direct** — direct/substantial code reuse is appropriate after file-level license review and attribution.
- **retain** — our existing implementation is preferable; use reference only for regression ideas.

Every completed row should eventually include concrete upstream path/commit evidence and a test/issue/PR reference.

## A. Guitar Pro parsing and symbolic timing

| Subsystem | Primary EOF reference | Our likely/current area | Status | Reuse | Priority | Notes / exit condition |
|---|---|---|---|---|---|---|
| GP3/GP4/GP5 binary parsing | `src/gp_import.c/.h` | `guitarpro_import.py`, source import | PARTIAL | port/direct | P0 | Compare version quirks, track data, durations, techniques and recovery semantics. |
| GP measure/time-signature model | `src/gp_import.c`, beat helpers | `guitarpro_import.py`, `source_import.py` | PARTIAL | port | P0 | Confirm measure positions survive import without hidden offsets. |
| Tempo events | `src/gp_import.c`, `src/beat.c/.h` | imported tempo events, `beats.py` | PARTIAL | port | P0 | Same GP must produce semantically equivalent source beat clock. |
| Explicit beat grid | `src/beat.c/.h` | `ImportedSource.beat_times_seconds` | PARTIAL | port | P0 | Compare rounding, anchors and beat-unit handling. |
| Leading rests/count-in/pre-roll | GP/GPA timing paths | `alignment.py`, onset refinement | PARITY | port | P0 | PR #413 adopted pre-zero handling; continue real packaged verification. |
| Chart delay / non-zero first beat | GP/GPA/project timing | alignment/project model | PARTIAL | port | P0 | Audit EOF cases where first beat is not 0 s. |
| GPA sync-point behavior | `src/gp_import.c/.h` | score→recording alignment | PARTIAL | port | P0 | Trace sync-point-to-project-beat semantics and edge cases. |
| Repeat unwrapping | `eof_unwrap_gp_track()` and helpers | `eof_repeat_unfolding.py` | PARTIAL | port | P0 | Advisory realized-sequence comparison landed (#462); the importer itself still does not unfold repeats (written score order only). |
| Alternate endings | GP measure/unwrapping logic | `eof_repeat_unfolding.py` | PARTIAL | port | P0 | Bitmask alternate-ending realization covered by the same #462 check. Da Capo/Segno/Coda/Fine navigation symbols are now also covered for GP5 sources; see the "Coda/segno/fine navigation" row directly below for the correction and its scope. |
| Coda/segno/fine navigation | `src/gp_import.c` (`eof_unwrap_gp_track()`, same function/commit as "Repeat unwrapping"/"Alternate endings" above): the `fileversion >= 500` 19-entry `gp->symbols[]` table (`src/gp_import.h`'s `EOF_CODA_SYMBOL`..`EOF_DA_DOUBLE_CODA_SYMBOL`) plus its destination-invalidation guard, and the Fine/Da Capo/Da Capo al Coda/Da Capo al Double Coda/Da Capo al Fine/Da Segno/Da Segno al Coda/Da Segno al Double Coda/Da Segno al Fine/Da Segno Segno/Da Segno Segno al Coda/Da Segno Segno al Double Coda/Da Segno Segno al Fine/Da Coda/Da Double Coda branches of the same unwrap loop, gated by `coda_activated`/`double_coda_activated`/`fine_activated` status flags | `eof_repeat_unfolding.py` (`extract_navigation_symbols()`, `has_navigation_symbols()`, `unfold_measure_sequence()`) | PARTIAL | port | P1 | This row's prior "no normalized table in PyGuitarPro's object model" citation was wrong and is corrected here: PyGuitarPro's GP5 reader (`gp5.py` `readDirections()`/`readMeasureHeaders()`) does normalize the identical on-disk table onto every `MeasureHeader` as `direction` (Coda/Double Coda/Segno/Segno Segno/Fine placements) and `fromDirection` (the 14 "from"/jump placements) -- EOF's own C comment confirms the format itself only defines this table for `fileversion >= 500` (GP5), so GP3/GP4 have no EOF navigation behavior to port either way, matching this project's own GP3/GP4 behavior (no `direction`/`fromDirection` data, unchanged). `unfold_measure_sequence()` now resolves the full 19-symbol branch chain exactly as audited, evaluated only when a measure is not itself an end-of-repeat with passes remaining (the same mutual-exclusion EOF's own loop enforces). Still PARTIAL, not PARITY: this remains the same advisory realized-sequence *comparison* as "Repeat unwrapping" above -- the generator's importer itself still does not unfold repeats or navigation symbols into actual note timing (written score order only); a GP5 score relying on Da Capo/Segno/Coda/Fine navigation is correctly reported as EOF-diverging evidence, not silently unfolded into canonical chart state. Verified with new regression tests in `tests/test_eof_repeat_unfolding.py` covering Da Capo, Da Segno (jumping to the Segno measure, not measure 0), Da Capo al Fine (stopping at the Fine measure on the second pass, not re-realizing measures after it), Da Capo al Coda (skipping an intervening written measure once Da Coda activates), the destination-invalidation guard (an orphaned Da Segno with no Segno anywhere is ignored), and GP3/GP4-style headers with no `direction`/`fromDirection` data at all (unchanged identity passthrough). |
| Triplet feel | `src/gp_import.c` (audited at current `raynebc/editor-on-fire` `master` commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`, per this row's own instruction to audit current upstream, not the pinned `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100` snapshot or the `xmist001` fork, given the May 2026 rewrite this row flagged): `eof_load_gp()` reads the GP measure-header triplet-feel byte only to print a debug-log line; the `tripletfeel` local variable it declares is never assigned the parsed value and is never read again -- EOF's own reference importer applies no timing/tick transformation for triplet feel, unchanged by the May 2026 rewrite | `guitarpro_import.py:convert_guitarpro_song()` (never reads `measure.header` at all) | PARITY | port | P1 | Closed, no gap: PyGuitarPro's own `Duration.time` derives a beat's tick length purely from its notated note value plus dotted/tuplet divisor, independent of `MeasureHeader.tripletFeel` -- a GP file's explicit beat-start ticks already encode whatever swing the tab author actually notated, and the triplet-feel flag is a notation/playback-humanization hint for GP's own renderer, not a stored timing offset. This project already matches EOF's own no-op reference behavior. Verified with a new regression test, `test_gp_import_ignores_measure_header_triplet_feel` (`tests/test_guitarpro_import.py`), locking down that imported note timing is unaffected by the measure header's `tripletFeel` value. See `docs/integrations/EOF_PARITY_ROADMAP.md` item 15. |
| Tied notes | GP import logic | source note construction + reviewed authoring | PARTIAL | port | P0 | Exact same-string/fret/pitch tie-only chains now fold at the reviewed Bass/Lead/Rhythm authoring boundary with continuation lineage, dual-clock adjacency, and composed source-track scoping. Different-string, mixed-technique, ambiguous, gap, overlap and linked-note behavior remain to audit. |
| Staccato/short-note truncation | `eof_load_gp()` note_is_short/truncate logic, `src/main.c` preference defaults | `eof_short_note_truncation_check.py`, `eof_export_boundary_check.py` | PARTIAL | port | P0 | Advisory check reproduces EOF's default-preference truncation decision (duration/staccato/mute/palm-mute, technique exemptions) and compares it to the generator's currently-untruncated imported sustains; the importer itself still does not apply truncation. `eof_export_boundary_check.py` re-applies the same decision against post-reconciliation/post-materialization notes, including a composed multi-track Lead/Rhythm arrangement (each contributing track's own facts matched only against notes resolved back to that same literal track), so a defect introduced after import, not just at import, is now covered too. A previously suspected previous-note legato/shift slide-in exemption was investigated against the pinned upstream commit and does not exist in EOF's import-time truncation decision. |
| Note endpoint resnapping/rounding | `gp_import.c` note-tail resnap pass (verbatim comment: "Resnap the end positions of notes that end 1ms after a grid snap position due to floating point math rounding error") | `eof_note_endpoint_resnap_check.py` | PARTIAL | port | P0 | Advisory check flags an imported note whose end sits within ~1ms of an imported beat-grid position without landing on it (the same narrow condition EOF's own pass corrects for). EOF's internal grid-snap helper (`eof_is_any_beat_interval_position`) was called from `gp_import.c`, `song.c`, and `src/menu/beat.c` but its own definition was not located in the accessible source tree; this check therefore compares against this project's own imported beat grid rather than assuming a finer EOF-internal subdivision. Advisory only (report, not auto-correct), consistent with this project's provenance/review-first checks. |
| Invalid GP notation recovery | `src/gp_import.c` (audited at the same current-master commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563` already cited by the "Triplet feel"/"Coda/segno/fine navigation" rows): `eof_load_gp()` aborts import when the header claims more than 5000 measures or 100 tracks ("Too many measures/tracks, aborting" -- a corrupt-or-hostile-file resource-exhaustion guard, not a format-quirk fallback), and separately detects and skips a spurious three-byte UTF-8 byte-order-mark prepended before the version string, a documented WebTabPlayer corruption pattern that would otherwise fail every version-string comparison | `guitarpro_import.py` (`import_guitarpro()`, `convert_guitarpro_song()`) | PARTIAL | port | P1 | Ported both audited behaviors. `import_guitarpro()` now detects and strips a leading UTF-8 BOM before handing the byte stream to PyGuitarPro (previously such a file failed to import at all, since the corrupted first bytes make every version-string match fail; PyGuitarPro raises `GPException` rather than crashing, so this was already a safe failure, but not a recovery), recording the recovery as a visible `ImportedSource` warning rather than silently rewriting the file. `convert_guitarpro_song()` now rejects a successfully-parsed song whose `tracks`/`measureHeaders` counts exceed EOF's own 100/5000 bounds before this project's own note-collection pass over the selected track walks it, mirroring EOF's resource-exhaustion rationale for our own downstream processing (PyGuitarPro itself performs no such bound check and a short/truncated file already fails safely with a natural read error once bytes run out, so this guards the case where the claimed counts are large but the file genuinely contains that much data). Closed PARTIAL, not PARITY: EOF's own separate notice-entry (>1000) and bend-point (>=100) count guards, and its broader "GP-newer-version quirks" handling (e.g. any fileversion-specific fallback behavior beyond the version-string lookup PyGuitarPro and this project's importer already reject outright for an unrecognized version), remain unaudited and unported. Four new regression tests (`tests/test_guitarpro_import.py`): a fake song object exceeding the track bound, one exceeding the measure bound, a real fixture file corrupted with a leading BOM importing successfully with the recovery warning present, and the same fixture without a BOM carrying no such warning. Not yet packaged-verified against a real corrupted or hostile Guitar Pro file. |

## B. Score-to-recording timing and authority

| Subsystem | Primary reference | Our area | Status | Reuse | Priority | Notes / exit condition |
|---|---|---|---|---|---|---|
| Project beat map as timing authority | EOF beat/project model | `alignment.py`, `shared_timeline.py` | PARTIAL | port | P0 | Same GP/audio must map notes once, not double-count intro/offset. |
| Pre-zero synchronized score beats | EOF GPA import | onset refinement | PARITY | port | P0 | Regression added in #413. |
| Global translation correction | EOF sync behavior + our audio evidence | onset refinement | PARITY+ | retain/port | P0 | EOF semantics plus evidence-driven automation. |
| Source timing qualification | no direct EOF equivalent | `source_timing_qualification.py` | PARITY+ | retain | P0 | Our stronger fail-closed gate remains. |
| Beat-grid validation | `src/beat.c` (audited at current `raynebc/editor-on-fire` `master` commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`): `eof_detect_tempo_map_corruption()` checks each beat individually -- it compares that beat's stored position against the position `eof_calculate_beat_pos_by_prev_beat_tempo()` derives from the *previous* beat's own tempo (`ppqn`), flags a beat "corrupt" if either its integer or floating-point position drifts past a small fixed tolerance (1ms / 0.5ms), and (chart unlocked) offers to recreate the tempo map from the beat positions | `alignment.py:align_source_to_tempo_map()` | PARTIAL | port | P0 | Found and fixed a real gap, not a missing citation: this project already computed per-region residual/confidence (`AlignmentRegion.rms_residual_seconds`/`max_abs_residual_seconds`/`confidence`) for every piecewise-linear anchor segment, but only ever checked the corpus-wide `median_abs_residual_seconds`/`confidence` against a warning threshold -- exactly the aggregate-only blind spot EOF's own per-beat design avoids, since a robust median is dominated by the majority of beats and can hide one bad segment. `align_source_to_tempo_map()` now also checks every region's own residual/confidence against the same thresholds already used for the aggregate check (RMS residual over 80ms, or confidence under 0.60) and emits a region-scoped warning naming the affected source-time span when either is exceeded, so a single bad segment can no longer hide behind a good overall median. Regression test `test_locally_corrupt_region_warns_even_when_global_median_is_clean` (`tests/test_alignment.py`) constructs a chart where 3 of 24 beats are pushed 0.3s late inside one region while every anchor point and every other beat stays exact, proving the median/RMS-based aggregate checks alone stayed silent (median residual well under 1ms) while the new per-region check still fires. Still PARTIAL, not PARITY: EOF's own GP-symbolic beat grid is a separately-stored, hand-editable `pos`/`fpos`/`ppqn` field set that can drift from tempo after a manual chart edit -- a corruption class this project's GP-imported symbolic timing structurally cannot exhibit, since `guitarpro_import.py` derives every note's time from tick position plus tempo points on every read rather than caching a separate position (see the "GP measure/time-signature model" and "Tempo events" rows above). The corruption class that *does* apply to this project is a bad symbolic-score/recording-audio beat match inside `align_source_to_tempo_map()`'s piecewise-linear alignment, which is what the new check covers. EOF's auto-correction path (recreate the tempo map from beat positions, or lock/unlock the chart) remains intentionally unported: this project fails closed with a named warning for human review rather than silently rewriting reviewed alignment data, consistent with this project's stronger provenance/human-review boundary (see the "Global translation correction" and "Source timing qualification" rows' own PARITY+ rationale). Not yet packaged-verified against a real Product Reality alignment defect. |
| Timing edits preserving anchors | EOF beat editing | reviewed timing workspace | PARTIAL | study/port | P1 | Compare invariant handling, not GUI mechanics. |
| Same timing authority for B/L/R | EOF project model / our architecture | shared timeline | PARITY+ | retain | P0 | Our single reviewed authority is deliberate architecture. |

## C. Note duration, gaps and sustain semantics

| Subsystem | EOF reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Basic note duration | GP import + song model | imported note duration | PARTIAL | port | P0 | Reviewed authoring now preserves exact folded tie-chain endpoints; broader GP duration/staccato parity remains. |
| Required inter-note gap | EOF note-gap/crazy status logic | arrangement generation/export | PARTIAL | port | P0 | `eof_note_gap_check.py` ports `song.c`'s `eof_get_note_max_length()` hard ceiling (a note's sustain may never extend past the position of the next note sharing its lane/string, LINKNEXT included) as an advisory check over `ReviewedExportArrangement` -- the post-reconciliation/post-materialization read model every export path consumes. Cross-string overlap (chords) and tied continuation notes (`techniques` contains `"tie"`) are deliberately excluded, not flagged. Normal validation now fails closed on any detected same-string overlap before export while still refusing to auto-trim musical authority; the importer itself does not rewrite durations. Also wired to a direct diagnostic entry point: `eof_note_gap_project.py` calls the project's own `reviewed_export_arrangement()` for the requested role and `cdlc-eof --check-note-gap` writes `review/eof_note_gap_report.json`, matching the rest-boundary/short-note-truncation/note-endpoint-resnap CLI wiring pattern; unlike those three (which reparse the raw registered GP score), this check reads the post-reconciliation/post-materialization arrangement directly, so it is meaningful for Bass, Lead, and Rhythm without a separate track-index argument. |
| Sustain to next note | EOF note/end logic | preview/export | PARTIAL | port | P0 | Same `eof_note_gap_check.py` check covers this: an overlapping same-string sustain would otherwise sound through what should be the next note's own attack. Must not create sustain through intended silence -- see also `eof_rest_boundary_check.py`/`eof_export_boundary_check.py` for the explicit-rest case, which this check does not evaluate. |
| Crazy/no-gap exception | `src/song.c` (audited at current `raynebc/editor-on-fire` `master` commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`, the same commit already cited by the "Beat-grid validation" and "Triplet feel" rows): `EOF_NOTE_FLAG_CRAZY` is force-applied to every note on `EOF_TRACK_KEYS` tracks and on BEATABLE-mode tracks (and to notes split from a disjointed chord, to keep other editor logic tolerant of the resulting overlap), and it relaxes `eof_get_note_max_length()`'s hard same-lane sustain ceiling only for those guitar-hero-style multi-lane note-highway tracks -- e.g. a crazy note that slides to another lane is explicitly allowed to overlap a note still sounding on its originating lane. `src/gp_import.c` (audited in the same pass) never sets or reads `EOF_NOTE_FLAG_CRAZY`: Guitar Pro import always produces `EOF_TRACK_PRO_GUITAR` notes, never `EOF_TRACK_KEYS`/BEATABLE notes, so the flag is architecturally never present on any note this project's own GP-import-derived pipeline would need to reproduce | N/A -- this project has no keys/BEATABLE multi-lane note-highway track type or its own guitar-hero-style overlap model | DIVERGENT | study | P1 | Traced but out of scope, not unassessed: "crazy" status is EOF's own guitar-hero-style multi-lane note-highway concept (keys/BEATABLE tracks and disjointed-chord splitting), not a Guitar Pro import or Rocksmith pro-guitar/string-fret concept -- confirmed absent from `gp_import.c` entirely. `eof_note_gap_check.py`'s own existing `eof_get_note_max_length()` port (see the "Required inter-note gap" row above) already documented this exact conclusion in its upstream-citation comment before this row was formally closed. This project's `SourceNoteEvent`/`ReviewedExportArrangement` models have no lane-highway or "crazy" concept and generate only Bass/Lead/Rhythm pro-guitar string/fret arrangements, so there is no Rocksmith-relevant behavior to port and no representable state to regression-test against. Revisit only if a future roadmap slice adds a non-pro-guitar (e.g. Guitar Hero-style) track type, which is not on the authorized roadmap. |
| Chord duration/truncation | `src/gp_import.c` (audited at `EOF_UPSTREAM_COMMIT` `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`): `eof_gp_import_truncate_short_notes` (default `1`) and `eof_gp_import_truncate_short_chords` (default `0`, `src/main.c`) are independent preferences gated by `eof_note_count_colors_bitmask(note) > 1`; EOF's two unconditional (duration-independent) truncation branches for full string-mute/palm-mute are further restricted to single notes only (`eof_note_count_colors_bitmask(note) <= 1`), so under EOF's own default preferences no branch can ever truncate a chord | `eof_short_note_truncation_check.py` (`EOF_DEFAULT_TRUNCATE_SHORT_NOTES`/`EOF_DEFAULT_TRUNCATE_SHORT_CHORDS`, `eof_truncation_decision()`'s `is_chord`/`truncate_short_chords` branch) | PARITY | port | P0 | This row was UNASSESSED despite the mapping already being implemented, tested, and CLI-wired (`cdlc-eof --check-short-note-truncation`) -- the same "citation never traced against EOF" gap previously found and fixed for the "Tremolo picking" row. Re-audited `gp_import.c`'s tie-note branch (`tie_note`/`vars.np[ctr2]->length`, ~4277-4284) for the "tied chord behavior" this row's own note called out: EOF never extends `length` for a tie continuation beat (only the first beat's note object is created and sized; ties merge flags/bend strength only), an architecture this project deliberately does not mirror -- `guitarpro_import.py` imports every beat (tie continuations included) as its own full-duration `SourceNoteEvent` tagged with the `"tie"` technique, and `reviewed_tie_folding.py` reconstructs the true chained duration later at the reviewed-authoring boundary, per-string/fret (so a tied chord folds exactly like a tied single note, one string at a time) -- see the "Tied notes" row above, which already tracks the remaining mixed-technique/cross-string tie-chain audit; there is no separate EOF chord-duration behavior left to port into this already-covered subsystem. Verified via existing coverage: `tests/test_eof_short_note_truncation_check.py::test_short_chord_is_not_truncated_under_default_preferences`, `::test_short_chord_is_truncated_when_chord_preference_is_explicitly_enabled`, `::test_muted_chord_is_not_truncated_because_it_is_not_a_single_note`. |
| Explicit rest boundary integrity | `eof_load_gp()` rest-beat-type byte | `eof_rest_boundary_check.py`, `eof_export_boundary_check.py` | PARTIAL | study | P0 | Advisory check cross-references every explicit-rest beat against imported note intervals for overlap; short-note/staccato/mute truncation is separately covered by `eof_short_note_truncation_check.py` (see "Staccato/short-note truncation" above). `eof_export_boundary_check.py` projects the same explicit-rest intervals through promoted reviewed timing and checks them against post-reconciliation/post-materialization notes, including a composed multi-track Lead/Rhythm arrangement (each contributing track's own rests checked only against notes resolved back to that same literal track), so a reconciliation/timing-projection step that stretches a sustain across a rest is now covered too; section-boundary parity remains unaudited (see roadmap item F). |

## D. Techniques

| Technique | EOF reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Hammer-on / pull-off | `src/gp_import.c` (`eof_load_gp()`: a single raw GP hammer/pull-off byte on the destination note; direction resolved by comparing that note's fret (`endfret`) to the immediately preceding same-string note's fret (`startfret`) -- higher sets `EOF_PRO_GUITAR_NOTE_FLAG_HO`, lower sets `EOF_PRO_GUITAR_NOTE_FLAG_PO`, audited at commit `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`); `src/rs.c` (exports separate `hammerOn`/`pullOff` note/chordNote XML attributes and sets the arrangement-level `hopo` property when either flag is present anywhere in the track) | `guitarpro_import.py:_resolve_hammer_pulloff_direction()`, `musicxml_import.py:_techniques()` (MusicXML already exposes explicit `<hammer-on>`/`<pull-off>` notations with no direction ambiguity), `rocksmith_xml.py` (`DIRECT_NOTE_TECHNIQUES`, `_technique_attributes()`, `hopo` arrangement property) | PARITY | port | P1 | Guitar Pro exposes only one ambiguous `NoteEffect.hammer` bit (`_techniques()`'s prior generic `"hammer_on_pull_off"` label); `_resolve_hammer_pulloff_direction()` now resolves it into the specific `"hammer_on"`/`"pull_off"` labels the MusicXML importer already emitted directly, using the identical previous-same-string-note fret comparison EOF's own GP import performs. A note with no preceding same-string note, or an equal fret, is left as the generic `"hammer_on_pull_off"` label and continues to fail closed at the Rocksmith XML export boundary, matching the unresolved-slide/bend pattern. Both importers' resolved labels now export as real `hammerOn="1"`/`pullOff="1"` note attributes plus the `hopo` arrangement property for Bass, Lead, and Rhythm, instead of failing the entire reviewed-authoring handoff (`ValueError: ... not losslessly supported yet`) as before -- this was a severe correctness gap, not a cosmetic one, since virtually every guitar/bass tab contains hammer-ons/pull-offs. Not yet packaged-verified against real Rocksmith import/playback. Tap (a distinct GP effect) remains separate and unaudited -- see the "Tap" row below. |
| Tap | GP import / RS | technique model | PARTIAL | port | P1 | Validate note/chord semantics. Related but distinct: PyGuitarPro exposes GP's beat-level tapping/slapping/popping indicator as `BeatEffect.slapEffect`, not a `NoteEffect` field; `raynebc/editor-on-fire`'s `eof_load_gp()` (audited at commit `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`) reads that same byte purely for a debug-log message and sets no `EOF_PRO_GUITAR_NOTE_FLAG_*` -- there is no mature EOF import-side mapping to port for GP-sourced slap/pop, so this remains a genuine GP-import gap, not yet attempted. The RS XML `slap`/`pluck` note attributes themselves are real and are now correctly round-tripped on PSARC re-import (see "Muted/string-muted notes" row below for the sibling `mute`/fret-hand-mute fix landed in the same change). RS XML `tap` (a distinct, non-boolean hand-direction attribute per `psarc_import._note_techniques()`) remains explicitly unsupported by this project's exporter (`test_specialized_bend_and_slide_do_not_duplicate_generic_warning` documents `"tap"` staying in the generic-unsupported set). |
| Palm mute | GP import / RS | technique model | PARTIAL | port | P1 | Preserve per-note/string semantics. |
| Harmonic / pinch harmonic | `gp_import.c` (harmonic-type byte 1=natural/2=artificial/3=tapped/4=pinch/5=semi, matched 1:1 to PyGuitarPro's `HarmonicEffect.type`; only type 1 sets `EOF_PRO_GUITAR_NOTE_FLAG_HARMONIC`, all others set `EOF_PRO_GUITAR_NOTE_FLAG_P_HARMONIC` under `eof_gp_import_nat_harmonics_only`'s default-off value in `main.c`), `rs.c` (exports separate `harmonic`/`harmonicPinch` XML attributes) | `guitarpro_import.py:_techniques()`, `rocksmith_xml.py` | GAP fixed | port | P1 | This was an active correctness bug, not just a missing-detail warning: every harmonic type (including pinch/artificial/tapped/semi) was previously exported as the plain `harmonic="1"` XML attribute, and `_ARRANGEMENT_PROPERTY_NAMES`'s existing `pinchHarmonics` property was never populated. Now: `_techniques()` tags only natural harmonics as `"harmonic"` and everything else as a new `"harmonic_pinch"` label (added to `reviewed_techniques.SUPPORTED_TECHNIQUES` and `rocksmith_xml.DIRECT_NOTE_TECHNIQUES`); `rocksmith_xml.py` emits the `harmonicPinch` note attribute and sets the `pinchHarmonics` arrangement property for it. |
| Tremolo picking | `src/gp_import.c` (audited at commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`): the GP note-effect "tremolo picking" bit (`byte2 & 4` in the relevant beat-effect decode path) sets `EOF_NOTE_FLAG_IS_TREMOLO` after discarding the accompanying tremolo-picking-speed sub-byte, which is read only via `pack_getc()` for a debug-log print and is never stored in EOF's internal note/song model or exported; `src/rs.c` exports that flag as a plain per-note `tremolo="0"/"1"` Rocksmith XML attribute (`ptr->tremolo = (flags & EOF_NOTE_FLAG_IS_TREMOLO) ? 1 : 0;`) and the arrangement-level `tremolo` property inside `<arrangementProperties>` -- no rate/speed field exists in either the EOF internal model or the RS2014 XML schema | `guitarpro_import.py:_techniques()` (`tremoloPicking is not None` -> `"tremolo_picking"`), `rocksmith_xml.py` (`tremolo` note attribute, `tremolo` arrangement property) | PARITY | port | P1 | This project's existing `NoteEffect.tremoloPicking`-is-not-None check already matches EOF's own GP-import mapping exactly, including the deliberate discard of PyGuitarPro's `TremoloPickingEffect.duration` (rate) value -- EOF discards the identical GP speed byte and RS2014's own `tremolo` attribute is a plain boolean with no rate field, so there is nothing lossy about not capturing it. Previously marked UNASSESSED despite the mapping already being implemented and exercised end-to-end (`tests/test_review_export_integration.py` already asserted the XML output); this row's own citation was simply never traced against EOF. Verified with a new import-side regression test, `test_gp_import_tags_tremolo_picking` (`tests/test_guitarpro_import.py`), covering the previously-untested `guitarpro_import.py` mapping directly (the existing XML-level test only exercised a hand-built `techniques` list, not the GP importer). Also see the "Tremolo phrases" row in section G below for EOF's separate phrase/region-editing mechanism, which does not add any additional exported RS XML behavior beyond this same per-note/arrangement boolean. Not yet packaged-verified against real Rocksmith import/playback. |
| Vibrato | `src/gp_import.c` (audited at `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`): every "vibrato"/"wide vibrato" occurrence in the file (note-level `byte2 & 64` and beat-level `byte1 & 1`/`byte1 & 2`) is inside a debug-log `puts()` call only -- EOF's own reference GP importer discards all GP vibrato information (note-level and beat-level) and never stores it in its internal note/song model, so there is no EOF import-side mapping to port; PyGuitarPro's own decoding (`gp3.py`/`gp4.py` `readBeatEffects()`) confirms the beat-level "wide vibrato" bit is stored on `BeatEffect.vibrato`, architecturally distinct from the per-note `NoteEffect.vibrato` bit `readNoteEffects()` sets | `guitarpro_import.py:_techniques()` | GAP fixed | retain | P1 | This project already exceeded EOF's own reference behavior by capturing per-note `NoteEffect.vibrato` and exporting it as the real Rocksmith `vibrato` note attribute plus arrangement-level `vibrato` property, but never consulted the separate beat-level `BeatEffect.vibrato` ("wide vibrato") field -- a beat marked wide-vibrato with no per-note vibrato bit on any of its notes silently imported with no vibrato technique at all, a genuine musical-correctness gap in this project's own logic (not an EOF parity gap, since EOF's default behavior discards vibrato entirely either way). Fixed: `convert_guitarpro_song()` now threads each beat's `BeatEffect.vibrato` into `_techniques()`, which tags every note in a wide-vibrato beat with `"vibrato"` regardless of that note's own per-note bit. Verified with `test_gp_import_tags_beat_level_wide_vibrato_when_no_note_carries_it` (`tests/test_guitarpro_import.py`). Intensity/strength reduction (GP has no separate vibrato-intensity concept surfaced by PyGuitarPro; Rocksmith XML's `vibrato` attribute is a fixed magic constant, not derived from GP) remains out of scope -- this row covers only the presence/absence gap. |
| Bend strength | `rs.c` (RS XML export: `step` attribute = quarter-step count / 2.0, i.e. semitones); PyGuitarPro's own GP decoding (`gp3.py:readBend`, already normalizes raw GP bytes to `BendPoint.position` 0..12 and `.value` in whole semitones, `bendSemitone=25`/`bendPosition=60`); public RS2014 XML `<note bend="1"><bendValues count="N"><bendValue time="..." step="..."/></bendValues></note>` schema (corroborated against `rscustom/rocksmith-custom-song-toolkit`'s community-documented Song2014 XML model; the `rs.c` excerpts inspected did not show the RS2 note-level XML serialization directly, only RS1-era `bendstrength` fields) | `guitarpro_import.py:_bend_points()`, `source_import.SourceBendPoint`, `rocksmith_xml.py` (`note_has_exportable_bend_curve`, `_bend_values`, `_append_bend_values`), `reviewed_export_events.py`/`reviewed_guitar_authoring.py`/`reviewed_bass_authoring.py`/`reviewed_rocksmith_xml.py`/`reviewed_rocksmith_xml_render.py` (additive `bend_points` field threaded through every reviewed-authority layer), `eof_rocksmith_validation.py` (`has_exportable_bend_curve` suppresses `rocksmith_bend_detail_missing` once the curve is actually exported) | PARITY | port | P0 | Full remaining scope from the previous entry landed: the captured curve now threads through every layer of the current reviewed-authority pipeline (`ReviewedExportNote` -> `ReviewedGuitarAuthoringNote`/`ReviewedBassAuthoringNote` -> `ReviewedRocksmithXmlNote` -> `GuitarAuthoringNote`/`MappedNote`) and is emitted as real `<bendValues>`/`<bendValue>` elements in `build_rocksmith_guitar_xml`/`build_rocksmith_bass_xml` (single notes and chord notes). `bend` is treated as exportable in `DIRECT_NOTE_TECHNIQUES`-gated checks only when the note actually carries curve points (`note_has_exportable_bend_curve`); a `"bend"` technique label with no captured points (e.g. from MusicXML/PSARC re-import, or a manual technique edit) still fails closed exactly as before, so no strength is ever invented. Verified with unit and end-to-end tests (`test_rocksmith_xml.py`, `test_guitar_rocksmith_xml.py`, `test_reviewed_rocksmith_xml.py`, `test_reviewed_rocksmith_xml_render.py`, `test_reviewed_guitar_authoring.py`, `test_reviewed_bass_authoring.py`, `test_eof_rocksmith_validation.py`); not yet verified against a real packaged Rocksmith import/playback (Product Reality evidence remains open -- see the "Bend timing" note below and the standing packaged-verification gate this repo tracks for all new authoring behavior). The legacy non-reviewed `guitar_authoring.build_guitar_authoring_chart` chart-building path and the audio-driven `fret_mapping.map_reconciled_bass_chart` reconciliation path were deliberately left out of scope: they are not the current reviewed-authority XML pipeline `reviewed_rocksmith_xml_render.py` calls, and a note reaching Rocksmith XML through either of them still carries no bend curve data (`bend_points` defaults to empty), so behavior there is unchanged, not regressed. Bend timing note: community documentation of adjacent toolkits describes a common one-third-of-sustain placement convention used when a tool lacks per-point data; this project has real per-point data from PyGuitarPro's own parsed curve, so `_bend_values()` uses each point's actual `position` fraction instead of that fallback heuristic. |
| Bend curve points | GP bend structures / RS | technique model | PARITY | port | P1 | Same landed change as the "Bend strength" row above; the two rows describe the same curve-fidelity work and could be merged in a future pass. |
| Slide in from above/below | `src/gp_import.c` reads the GP slide-type byte but discards it (no EOF import-side mapping exists, confirmed at commit `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`); PyGuitarPro `guitarpro.models.SlideType` (`intoFromAbove`/`intoFromBelow`) used instead | `guitarpro_import.py:_slide_kinds()`, `SourceNoteEvent.slide_kinds` | PARTIAL | study | P0 | Import-side data preservation landed (all six PyGuitarPro subtypes captured in `slide_kinds`); this row covers only the target-less "into"/"out" subtypes (`intoFromAbove`/`intoFromBelow`/`outDownwards`/`outUpwards`), which have no defined destination fret to resolve or export -- see the "Pitched slide"/"Unpitched slide" rows below for the two subtypes ("shift"/"legato") that do. EOF audit finding unchanged: `gp_import.c` never maps the GP slide-type byte to any note flag (log-only), so there is no EOF import-time behavior to port here. |
| Pitched slide | `src/rs.c` (RS XML export: `slideTo` attribute = destination fret); `src/rs_import.c` populates `EOF_PRO_GUITAR_NOTE_FLAG_SLIDE_UP`/`_SLIDE_DOWN` from RS XML's `slideTo` on re-import (this project's own `psarc_import.py` already reads the same `slideTo` attribute on re-import, corroborating the attribute name/shape); PyGuitarPro `SlideType.shiftSlideTo`/`legatoSlideTo` used for import-side subtype capture | `guitarpro_import.py:_resolve_slide_target_frets()`, `source_import.SourceNoteEvent.slide_target_fret`/`link_next`, `rocksmith_xml.py` (`note_has_exportable_slide_target`, `slideTo`/`linkNext` attributes in `_technique_attributes`), `reviewed_export_events.py`/`reviewed_guitar_authoring.py`/`reviewed_bass_authoring.py`/`reviewed_rocksmith_xml.py`/`reviewed_rocksmith_xml_render.py` (additive `slide_target_fret`/`link_next` fields threaded through every reviewed-authority layer, mirroring `bend_points`), `eof_rocksmith_validation.py` (`has_exportable_slide_target` suppresses `rocksmith_slide_detail_missing` once the target is actually exported) | PARITY | port | P0 | The "shift"/"legato" pitched-slide subtypes now resolve their implicit Guitar Pro destination (GP encodes it only as "the next note on this string", not as an explicit value PyGuitarPro exposes) and export it losslessly as the Rocksmith XML `slideTo` attribute plus the `slides` arrangement property, threaded through every layer of the reviewed-authority pipeline exactly like the bend-curve work above. A slide note with no later same-string note to resolve against (e.g. the last note on that string) is left unresolved and continues to fail closed (`rocksmith_slide_detail_missing`), same as an unresolved bend. Verified with unit tests (`test_guitarpro_import.py`, `test_rocksmith_xml.py`, `test_reviewed_rocksmith_xml.py`, `test_reviewed_export_events.py`, `test_reviewed_guitar_authoring.py`, `test_eof_rocksmith_validation.py`); not yet verified against a real packaged Rocksmith import/playback (Product Reality evidence remains open, per the standing packaged-verification gate this repo tracks for all new authoring behavior). Correction to a prior version of this row: link-next state *is* a separate RS XML attribute (`linkNext`), and EOF's own `gp_import.c` (audited at commit `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`) maps only the "legato" pitched-slide subtype's GP slide-type bit to `EOF_PRO_GUITAR_NOTE_FLAG_LINKNEXT` -- a "shift" slide never sets it. This was a genuine gap (every resolved pitched slide exported identically regardless of subtype, silently dropping the legato/shift distinction Rocksmith actually encodes) fixed in the same family as the harmonic-type fix above: a resolved "legato" slide now also sets the new additive `link_next` field, exported as `linkNext="1"` alongside `slideTo`; a resolved "shift" slide never does. Direction (up/down) is still not a separate RS XML attribute and remains out of scope. |
| Unpitched slide | GP + RS | technique model | PARTIAL | port | P0 | Subtype captured at import (see "Slide in from above/below" row); the `outDownwards`/`outUpwards` subtypes have no PyGuitarPro-exposed target fret to resolve (unlike shift/legato above) and remain unresolved/unexported -- `slide_target_fret` stays unset and export continues to fail closed via `rocksmith_slide_detail_missing`. |
| Shift vs legato slide | PyGuitarPro `SlideType.shiftSlideTo`/`legatoSlideTo`; `src/gp_import.c` maps only the legato bit to `EOF_PRO_GUITAR_NOTE_FLAG_LINKNEXT` | `guitarpro_import.py:_slide_kinds()`, `_resolve_slide_target_frets()` | PARITY | port | P1 | Both subtypes are distinguished at import via `slide_kinds`, and both now resolve/export the same `slideTo` value (see "Pitched slide" row above) -- but they are no longer exported identically: a resolved legato slide additionally sets `link_next`/`linkNext="1"`, a resolved shift slide does not, matching EOF's own GP-import behavior exactly (see "Link-next" row and the "Pitched slide" row's correction note). |
| Link-next | `src/rs.c` (`eof_rs2_export_note_string_to_xml()` emits the `linkNext` note/chordNote attribute from `tech.linknext`, itself read from `EOF_PRO_GUITAR_NOTE_FLAG_LINKNEXT`); `src/gp_import.c` sets that flag from the GP slide-type byte only for the "legato" pitched-slide subtype (never "shift"), and separately for tie notes that introduce a new technique not already present on the note they would otherwise fold into, and for grace-note slide-ins (both out of scope here) | `source_import.SourceNoteEvent.link_next`, `guitarpro_import.py:_resolve_slide_target_frets()`, `rocksmith_xml.py` (`linkNext` attribute in `_technique_attributes`), the same reviewed-authority layers as `slide_target_fret` | PARTIAL | port | P1 | Fixed for the legato-pitched-slide case as part of the "Pitched slide" row's correction: a resolved legato slide now exports `linkNext="1"` alongside `slideTo`, a resolved shift slide does not, and an unresolved slide exports neither (never invented without the concrete `slideTo` it describes). Verified with unit tests (`test_guitarpro_import.py`, `test_rocksmith_xml.py`, `test_guitar_rocksmith_xml.py`, `test_reviewed_export_events.py`, `test_reviewed_guitar_authoring.py`, `test_reviewed_rocksmith_xml.py`); not yet packaged-verified. Remains PARTIAL, not PARITY: EOF's tie-note-introduces-a-new-technique case (`gp_import.c` ~4119) and grace-note slide-in case (~4345) are separate `EOF_PRO_GUITAR_NOTE_FLAG_LINKNEXT` sources this project does not yet derive `link_next` from -- see the "Tied notes" row in section A (mixed-technique tie chains are explicitly still unaudited) and the ungraded "grace" technique. |
| Ghost notes | `src/gp_import.c` (audited at `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`): ghost status for a non-drum track is retained only when `eof_gp_import_keep_ghost_guitar_status` is enabled, which `src/main.c` defaults to `0` (off) -- EOF's own default behavior discards GP ghost-note status entirely for guitar/bass and imports the note as ordinary; `src/rs.c`: even when EOF does retain ghost status, it is never exported as any Rocksmith XML note attribute (no such attribute exists in the RS2014 format) -- it is consumed purely internally by `eof_is_partially_ghosted()` and the "partially ghosted chord" splitting logic to decide which gems count toward EOF's own chord/single-note/arpeggio-handshape export shape, a per-chord-gem concept this project's per-note technique model has no equivalent for | `guitarpro_import.py:_techniques()` | GAP fixed | port | P1 | Previously this project tagged every GP ghost-flagged note with a generic `"ghost_note"` technique, in `reviewed_techniques.SUPPORTED_TECHNIQUES` but not `rocksmith_xml.DIRECT_NOTE_TECHNIQUES` -- forcing an `unsupported_imported_technique` human-review warning on every ghosted Bass/Lead/Rhythm note even though EOF's own default reference behavior imports it identically to an unghosted note, and even EOF's own opt-in retained-ghost path never exports a comparable RS XML attribute. `_techniques()` no longer maps GP's `ghostNote` effect to any technique label, matching EOF's own default (`eof_gp_import_keep_ghost_guitar_status = 0`); `"ghost_note"` was removed from `SUPPORTED_TECHNIQUES` since no importer produces it. Verified with `test_gp_import_does_not_tag_ghost_note_as_a_technique` (`tests/test_guitarpro_import.py`). EOF's internal per-gem chord/handshape/arpeggio ghost-exclusion logic itself remains out of scope -- this project's note model has no per-chord-gem structure to port it into; see the "Handshape/arpeggio creation" V3 item in `docs/eof-rocksmith-validation-rule-pack.md` if that becomes relevant later. |
| Muted/string-muted notes | RS/fingering logic | note model | PARTIAL | port | P1 | Interacts with fingering/FHP. Fixed a real PSARC round-trip regression: `psarc_import._note_techniques()` already read the RS XML `mute` (fret-hand-mute), `slap`, and `pluck` note attributes but produced technique labels (`"pinch_harmonic"` for `harmonicPinch`; `"fret_hand_mute"`/`"slap"`/`"pluck"` themselves) that neither `reviewed_techniques.SUPPORTED_TECHNIQUES` nor `rocksmith_xml.DIRECT_NOTE_TECHNIQUES` recognized -- a note re-imported from an already-valid Rocksmith PSARC would fail review acceptance or silently fail to re-export those attributes. Also fixed: the `slap`/`pluck` attributes are ternary in real RS2014 XML (`-1` not applicable, `0` applicable-but-not-played, `1` played), matching the sibling `tap` attribute's own three-way check in the same function, but the `slap`/`pluck` checks previously excluded only `"-1"`, misreading every explicit "not slapped"/"not plucked" bass note (`"0"`) as carrying the technique. All four labels (`fret_hand_mute`, `slap`, `pluck`, and the `harmonic_pinch` rename) are now registered end-to-end (`SUPPORTED_TECHNIQUES`, `DIRECT_NOTE_TECHNIQUES`, `_technique_attributes()` re-exporting `mute="1"`/`slap="1"`/`pluck="1"`, and the `fretHandMutes`/`slapPop` arrangement properties). GP-side import producing these techniques from scratch remains out of scope (see the "Tap" row above for the EOF audit finding on GP's beat-level slap/pop byte). |

## E. Tuning, strings and position

| Subsystem | EOF reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Track tuning import | GP import/tuning helpers | source track tuning | PARTIAL | port | P0 | Compare string order and pitch interpretation. |
| Capo | `src/gp_import.c` (reads the GP capo DWORD, debug-logged as `"Capo: Fret N"`/`"(none)"`, audited at commit `c0d88eabf7b00b0bd2cac9414df9fa9c6b3e7100`); `src/rs.c`'s `eof_export_rocksmith_2_track()` for RS2014 specifically writes the top-level `<capo>%u</capo>` tag from `tp->capo` and adds it to each chord-template string's fret value (`fret += tp->capo; //Apply the capo position`), while leaving individual per-note/`chordNote` `fret` attributes unmodified -- RS2014 represents the capo once via the top-level tag, unlike RS1 | `source_import.SourceTrack.capo`, `guitarpro_import.py` (`Track.offset`), `reviewed_export_events.ReviewedExportArrangement.capo`, `reviewed_bass_authoring.py`/`reviewed_guitar_authoring.py` (`capo` threaded alongside `tuning_midi`), `reviewed_rocksmith_xml.ReviewedRocksmithXmlInput.capo`, `reviewed_rocksmith_xml_render.py` (`BassMapping.capo`/`GuitarAuthoringChart.capo`), `rocksmith_xml.py` (`<capo>` tag and chord-template `fret{N}` offset) | PARITY | port | P1 | Was a confirmed correctness bug: `rocksmith_xml.py`'s `<capo>` tag was hardcoded to `0` regardless of the source Guitar Pro file's actual capo setting, so any capoed song round-tripped fret data incorrectly. Fixed for Guitar Pro import: `guitarpro_import.py` now reads `Track.offset` (PyGuitarPro's capo-fret field; see its `gp3.py` `readTrack()` docstring) into `SourceTrack.capo` and threads it, arrangement-level (once per Bass/Lead/Rhythm arrangement, not per note, matching `tuning_midi`'s existing precedent), through the reviewed-authority pipeline to the real `<capo>` tag and chord-template `fret{N}` values, matching EOF's RS2014 export exactly; individual note/`chordNote` `fret` attributes are confirmed and regression-tested to stay untouched by capo (the asymmetry EOF's own code exhibits). Verified with unit tests (`test_guitarpro_import.py`, `test_rocksmith_xml.py`, `test_guitar_rocksmith_xml.py`, `test_reviewed_export_events.py`, `test_reviewed_bass_authoring.py`, `test_reviewed_guitar_authoring.py`, `test_reviewed_rocksmith_xml.py`, `test_reviewed_rocksmith_xml_render.py`); not yet packaged-verified against real Rocksmith import/playback. MusicXML and PSARC re-import capo handling remain out of scope for this slice -- EOF's own capo handling is GP-specific too, so there is no mature behavior to port for those importers yet. The separate, non-reviewed alignment-based CLI pipeline (`fret_mapping.map_bass_transcription()`, `guitar_authoring.build_guitar_authoring_chart()`, driven by `mapping_pipeline.py`/`cli.py`) gained the same additive `capo` field on `BassMapping`/`GuitarAuthoringChart` (default `0`, required by the shared `rocksmith_xml.py` builders) but was not threaded from `SourceTrack.capo` in this slice; it still always exports capo `0`, unchanged from prior behavior. |
| String index preservation | GP import | source note | PARTIAL | port | P0 | Never silently remap when explicit source position exists. |
| Fret preservation | GP import | source note | PARTIAL | port | P0 | Same as above. |
| 7-string/extended range handling | EOF GP logic | importer | UNASSESSED | study/port | P2 | Decide RS2014 supported reduction policy explicitly. |

## F. Chords, fingering, FHP and handshapes

| Subsystem | EOF reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Chord template parsing/import | `src/rs_import.c` | chord identity/model | PARTIAL | port/direct | P0 | EOF parses Rocksmith finger/fret templates; useful oracle. |
| Chord identity | RS/chord helpers | reviewed chord identity | PARTIAL | port | P0 | Compare membership/name/shape semantics. |
| Fingering inference | `src/bf.c/.h`, RS helpers | fret mapping/review | PARTIAL | port/direct | P0 | High human-editing leverage. |
| Fingering validation | EOF fingering view/RS warnings (`eof_pro_guitar_note_fingering_valid()`/`eof_note_exports_without_fingering()`, `src/rs.c`, audited at commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`) | `rocksmith_xml._chord_template_fingers()` | PARTIAL | port | P0 | Confirmed a real, previously-undocumented gap: `guitarpro_import.py` read no per-note fingering data at all, and `rocksmith_xml.py` unconditionally hardcoded every `chordTemplate` `finger0`..`finger5` attribute to `-1` regardless of source data. PyGuitarPro's `Note.effect.leftHandFinger` (a `Fingering` enum whose values already match Rocksmith's own "0 = thumb".."4 = little" numbering directly, confirmed against `src/gp_import.c`'s identical raw-byte read and `src/rs.c`'s thumb-sentinel conversion back to 0) is now captured as `SourceNoteEvent.left_hand_finger` and threaded through the full reviewed-authority pipeline (Bass included, for structural parity with the shared `ReviewedRocksmithXmlNote` adapter, though Bass has no chordTemplate finger export to consume it). `_chord_template_fingers()` ports EOF's validity/fallback algorithm: a chord's representative-instance fingering is used only when fully defined for every fretted, non-muted string the shape uses (muted strings exempted, matching EOF's default `eof_fingering_checks_include_mutes = 0`; an open string carrying a spurious fingering invalidates the whole chord, matching `eof_pro_guitar_note_fingering_valid()`); otherwise every string exports `-1`, matching `eof_note_exports_without_fingering()`'s fallback. Closed PARTIAL, not PARITY: EOF's predefined chord-shape fingering library (`eof_lookup_chord_shape()`) is not ported (this project has no equivalent, so an incomplete per-note fingering always takes the "no fingering" fallback rather than a library-matched one), Guitar Pro's separate per-string chord-diagram fingering (`Chord.fingerings`, attached to a beat's visual chord-box rather than a specific note) is not read, EOF's explicit per-note "fingerless" designation has no equivalent here, and multi-instance fingering-consistency checking across repeated occurrences of one chord shape is not ported (matches EOF's own single-representative-instance export algorithm, so this is not a gap, just a documented scope boundary). Ten new regression tests (`test_guitarpro_import.py`, `test_guitar_rocksmith_xml.py`) cover import capture (including thumb=0 vs. open=unset) and chordTemplate export (complete/partial/open-string-invalid/muted-exempt fingering). Not yet packaged-verified against real Rocksmith import/playback. |
| Fingerless/muted rules | `src/rs.c` (audited at commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`): `EOF_PRO_GUITAR_NOTE_EFLAG_FINGERLESS` is a per-note flag checked by `eof_pro_guitar_note_fingering_valid()`/`eof_note_exports_without_fingering()` (both return "valid"/"exports without fingering" immediately when set); confirmed via `grep` of the same-commit `src/gp_import.c` that Guitar Pro import never sets it -- it is set only by manual user action in EOF's own note-properties editor UI, with no GP-file-derived signal to import | `guitar_validation.guitar_chart_rule_findings()`/`rocksmith_xml.chord_exports_without_fingering()` | PARTIAL | port | P1 | No GP-import-time behavior to port: this project's Guitar Pro importer has no source signal analogous to a manual "mark this note fingerless" designation, so there is nothing to thread through `SourceNoteEvent` the way `left_hand_finger` itself was (issue #414, PR #599); adding an equivalent manual per-note override would be a new reviewed-authoring UI feature, not a parity port, and needs its own product scoping. Auditing this row's actual "avoid unnecessary review requirements" goal surfaced a real, separate bug instead: `guitar_validation.py`'s `rocksmith_chord_fingering_missing` warning (`eof_rocksmith_validation.guitar_chart_rule_findings()`) unconditionally fired whenever a chart contained any chord, unconditionally claiming "chord fingering is not yet modeled/exported" even after PR #599 made real fingering export possible -- so a chord with fully-defined imported fingering still triggered an unnecessary review warning. Fixed by extracting the existing chordTemplate completeness test into a new `rocksmith_xml.chord_exports_without_fingering()` (reused by `_chord_template_fingers()` so both stay in sync by construction) and having `guitar_validation.py` pass the count of chords that actually lack exportable fingering instead of the raw chord count. New regression coverage: `test_chord_exports_without_fingering_matches_template_finger_completeness` (`tests/test_guitar_rocksmith_xml.py`), `test_fully_fingered_chord_does_not_warn_missing_fingering` (`tests/test_guitar_validation_export.py`), plus updated `tests/test_eof_rocksmith_validation.py` parameter coverage. Not yet packaged-verified against real Rocksmith import/playback. |
| Fret-hand positions | `src/rs.c`, song/track helpers | arrangement authoring | PARTIAL | port/direct | P0 | Audit placement, width, violations and generation. |
| FHP range/width | EOF fret-range tolerance logic (`src/rs.c` `eof_note_can_be_played_within_fret_tolerance`, `src/note.c` lowest/highest-fret helpers) | `eof_fret_range_tolerance_check.py` | PARTIAL | port | P0 | Default-preference uniform-4-fret geometric compatibility test ported and advisory-wired (`cdlc-eof --check-fret-range-tolerance`, issue #414). Confirms a real gap: `rocksmith_xml.py` always writes `<anchors count="0"/>`, so any arrangement with fretted content is missing every structurally required fret-hand-position. Explicitly excluded: the barre-chord same-position exemption, EOF's dynamic/user-widened tolerance tables, and the beat-level tap/slap/pop exemption. |
| Handshape/arpeggio phrases | RS/song/track logic | arrangement phrases | PARTIAL | port | P0 | Include chord-slide transitions. |
| Handshape/FHP violations | EOF RS panel checks | validation | PARTIAL | port | P0 | `eof_fret_range_tolerance_check.py` supplies the geometric-tolerance slice of this row (see FHP range/width above). The fingering/slide/arpeggio-phrase/capo/RS-phrase-boundary logic in `eof_generate_efficient_hand_positions_logic()` that decides additional position changes and exact FHP placement remains unaudited and is the next slice. |

## G. Phrases, sections and Rocksmith events

| Subsystem | EOF reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Rocksmith section vocabulary | `src/rs.c` predefined sections | section model | PARTIAL | direct/port | P1 | EOF has explicit canonical section set. |
| Phrase boundaries | beat/RS helpers | phrase model | PARTIAL | port | P1 | Compare edge constraints and repetition semantics. |
| COUNT phrase / leading count measure | `src/rs.c::eof_rs_export_common()` (audited at commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`): unconditionally inserts a project-wide, case-sensitive `"COUNT"` RS phrase at beat 0 and an `"END"` RS phrase at the beat following the track's last note whenever the user has not manually defined an equivalent (`eof_song_contains_event(sp, "COUNT"/"END", track, EOF_EVENT_FLAG_RS_PHRASE, 2)`, `eof_song_add_text_event(..., is_temporary=1)`) -- a fixed structural export fallback, not a musical/authoring decision | `rocksmith_xml._build_common_song_header()` | GAP | port | P1 | See `docs/eof-count-phrase-audit.md`. Confirmed a real, previously-undocumented gap: this generator's shared header builder emits exactly one placeholder `<phrase name="song">`/`<phraseIteration>` per arrangement and never a `COUNT` phrase at the first beat or an `END` phrase at the end of the track, for Bass, Lead, or Rhythm. `COUNT`/`END` are fixed literal names required regardless of song content (unlike the sibling "Section validation" row's arbitrary musical section vocabulary), so implementing them needs no reviewed musical authority. Not implemented in this audit: the fix touches the same `_build_common_song_header()` function and placeholder-`"song"`-name pattern that PR #606's open "Section validation" audit is still resolving, so the code change is deferred to be sequenced with (or immediately after) that redesign rather than landed as a conflicting parallel edit. |
| Tick events | EOF leading silence/RS events | event model | UNASSESSED | port | P2 | Needed if downstream RS semantics require. |
| Tremolo phrases | `src/gp_import.c` (`eof_build_tremolo_phrases()`, called once per lead/rhythm/all-difficulty voice right after GP import) and `src/song.c` (`eof_determine_phrase_status()`, called before export to resync each note's tremolo status) | N/A -- no phrase/region-editing surface in this project's product model | DIVERGENT | study | P2 | Traced but out of scope, not unassessed: this is EOF's own chart-editor convenience feature, letting a human draw/extend a "tremolo phrase" region in EOF's timeline UI after GP import, which `eof_determine_phrase_status()` then uses to resync each contained note's `EOF_NOTE_FLAG_IS_TREMOLO` bit before export. It does not add any Rocksmith XML output beyond the same per-note `tremolo` attribute and arrangement-level `tremolo` property already covered by the "Tremolo picking" row above (`src/rs.c`'s export call sites are identical either way) -- it only changes how a human can edit which notes carry that flag inside EOF itself. This project has no equivalent manual phrase-region review/editing surface, and none is on the authorized roadmap (see `docs/eof-subsystem-parity-matrix.md`'s "Tremolo picking" row and issue #414); a note's `tremolo_picking` technique is set purely from the imported GP data, matching EOF's own default (pre-manual-edit) import behavior. Revisit only if a future roadmap slice adds phrase/region-level review. |
| Handshape phrases | EOF RS logic | handshape model | PARTIAL | port | P0 | See chord/FHP phase. |
| Section validation | EOF warnings | validation | UNASSESSED | port | P1 | Convert warnings into deterministic checks. |

## H. Difficulty and dynamic difficulty

| Subsystem | Reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| Static difficulty populations | EOF track difficulties | arrangement model | UNASSESSED | study/port | P2 | Understand before generating levels. |
| Difficulty tabs/population semantics | EOF | desktop model | N/A/learn | study | P3 | GUI mechanics not required; domain state may matter. |
| Dynamic difficulty generation | Rocksmith Custom Song Toolkit DDC | future DD | UNASSESSED | study/port/direct after license audit | P1 | Audit `RocksmithTookitGUI/DDC`, `DLCPackage/DDCreator.cs`, configs. |
| Chord protection/removal rules | toolkit DDC configs | future DD | UNASSESSED | study/port | P2 | Evaluate intended learning progression. |

## I. Rocksmith XML import/export and validation

| Subsystem | Reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| RS2014 XML import | EOF `src/rs_import.c` | import/reference tools | UNASSESSED | port/direct | P2 | Useful as oracle and round-trip fixture source. |
| RS2014 section/event export | EOF `src/rs.c` | XML authoring | PARTIAL | port/direct | P0 | Compare semantic output. |
| Chord templates | EOF + toolkit `Song2014.cs` | XML authoring | PARTIAL | port | P0 | High-value parity target. |
| Notes/chords XML fields | EOF + toolkit XML model | XML authoring | PARTIAL | port | P0 | Differential semantic tests. |
| Arrangement metadata | EOF RS arrangement model + toolkit | XML/package model | PARTIAL | port | P1 | Lead/Rhythm/Bass metadata must match RS expectations. |
| Phrase iterations/levels | toolkit XML model/DDC | XML authoring | UNASSESSED | port | P1 | Needed before robust dynamic difficulty. |
| Export warnings/validation | EOF RS checks | validation | PARTIAL | port | P0 | Mature warning rules should become deterministic blockers/warnings. |
| Tempo warning thresholds | `src/menu/file.c` (audited at commit `4a724f4b068b4dd11a71a4b688707a0ed35b6563`, the same commit already cited by several other closed rows): `eof_check_tempo_range(40.0, 300.0)` (`src/beat.c`) scans every beat's instantaneous tempo (`60000000.0 / ppqn`) and, when saving a Rocksmith-capable file and the check is not suppressed (`eof_rs_export_suppress_tempo_warning`, `src/main.c`), asks the user to confirm or cancel the save if any beat falls outside 40-300 BPM; the RS2014 XML/SNG formats themselves enforce no such bound, so this is EOF's own editor-side sanity check against likely tempo-detection/authoring mistakes, not a hard Rocksmith import constraint | `eof_rocksmith_validation.tempo_range_rule_finding()`, consumed per-beat by `validation.py` (Bass) and `guitar_validation.py` (Lead/Rhythm) | PARTIAL | port | P2 | Decided the RS-constraint-vs-editor-convenience question this row asked: confirmed via `src/menu/file.c` that the 40-300 BPM check is a cancel-or-continue advisory dialog, not a hard save-blocking validation, so it is ported as a non-blocking `WARNING` review finding (`rocksmith_tempo_out_of_range`) rather than a packaging `FAIL`, on every one of this project's already-computed per-beat `TempoMap.beats[i].bpm` values (the same instantaneous-tempo quantity EOF's own check reads). Wired identically into both the Bass (`validation.py`) and guitar (`guitar_validation.py`) tempo-validation loops to preserve Bass/Lead/Rhythm as independent first-class arrangements. Closed PARTIAL, not PARITY: unlike EOF's single early-exit save-time dialog, every offending beat is reported (a presentation difference, not a behavioral one), and EOF's own suppress-preference has no analog here since this project has no persistent user preferences file for validation warnings. New regression coverage: `test_tempo_within_eof_advisory_range_does_not_warn`/`test_tempo_below_eof_advisory_range_warns`/`test_tempo_above_eof_advisory_range_warns` (`tests/test_eof_rocksmith_validation.py`), `test_out_of_range_tempo_beat_produces_advisory_warning` (`tests/test_validation.py`), `test_guitar_validation_warns_on_out_of_eof_tempo_range` (`tests/test_guitar_validation_export.py`). Not yet packaged-verified against real Rocksmith import/playback of a chart with an extreme tempo change. |

## J. SNG, package and downstream tooling

| Subsystem | Reference | Our area | Status | Reuse | Priority | Notes |
|---|---|---|---|---|---|---|
| SNG2014 writing | toolkit `Sng2014FileWriter.cs` | PSARC/SNG bridge | PARTIAL | study/port subject to license | P1 | Compare structures with existing bridge; do not replace blindly. |
| Arrangement package model | toolkit `DLCPackage/Arrangement.cs` | package model | PARTIAL | study | P1 | Check metadata/IDs/relationships. |
| Aggregate graph | toolkit `AggregateGraph2014` | package generation | UNASSESSED | study | P2 | Audit only if relevant to our bridge. |
| DDC invocation/config | toolkit DDC | future DD/build | UNASSESSED | study/port | P1 | Establish behavior and licensing. |
| Toolkit external app integration | toolkit | DLC Builder handoff | PARTIAL | study | P2 | Learn resilient discovery/handoff patterns. |

## K. Product/UX concepts worth learning but not porting wholesale

| Area | Reference value | Decision |
|---|---|---|
| Piano-roll editor | Mature visual debugging of note/beat relationships | LEARN ONLY; our UI remains modern Windows authoring workspace. |
| FHP/fingering visualization | Clear explanation of playability violations | PARITY+ opportunity; adopt concepts with our design system. |
| Rocksmith status panels | Mature warnings close to authoring context | PARITY+ opportunity; map to our project health/validation surfaces. |
| Manual beat editing | Excellent reference for invariants | LEARN ONLY; our automation/review model remains different. |
| Undo/redo mutable editor state | Useful behavior reference | INTENTIONAL DIVERGENCE; retain transactional provenance-aware review state. |

## First audit sequence

The next detailed audit should proceed in this order:

1. GP import + source beat clock.
2. note duration/gap/sustain semantics.
3. repeats/ties/alternate endings/triplet feel.
4. slide and bend semantics.
5. chord/fingering/FHP/handshape.
6. Rocksmith phrase/section/event validation.
7. XML semantic differential tests.
8. toolkit/DDC/package comparison.

Do not start with broad GUI imitation. The value is in deterministic domain correctness first.

## Fork evidence rule

A fork is referenced as a distinct implementation only after commit/branch comparison proves unique behavior. `xmist001/editor-on-fire-automated` is currently a May 2026 snapshot of primary EOF, not a separate automated lineage.
