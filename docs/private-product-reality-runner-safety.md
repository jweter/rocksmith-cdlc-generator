# Private Product Reality Runner safety boundary

The runner never treats private source bytes as CI fixtures. It operates locally against private lawful projects and emits derived evidence only.

Fail closed on missing/stale hashes or authority. Never convert absence of evidence into PASS.

Song-specific expectations belong only in private scenario configuration and are never generation corrections.
