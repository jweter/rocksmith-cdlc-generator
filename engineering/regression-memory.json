{
  "schema_version": 1,
  "repository": "jweter/rocksmith-cdlc-generator",
  "entries": [
    {
      "id": "ci-log-and-partial-authority-fixture-2026-09-06",
      "status": "verified_repair_in_flight",
      "root_cause": "Two coupled process defects: (1) CI triage did not descend workflow -> failed job -> job logs before declaring evidence unavailable; (2) narrow routing tests used a partial reviewed-authority fixture and mocked only the exporter consumer, so a newly added independent validator consumed the same marker and changed the fixture's meaning.",
      "regression_protection": [
        "tests/test_authoring_export_reviewed_routing.py",
        "src/rocksmith_cdlc_generator/eof_note_gap_validation.py"
      ],
      "verification": "On exact head b756fdb387d35237ba711aafeecd0528cd22df88, CI and Windows Desktop are GREEN; only Control Plane failed because this regression-memory entry violated its schema. The routing-test fixture repair itself passed the full product test suite.",
      "residual_risk": "Future validators may consume shared partial-authority fixtures in unrelated unit tests unless those tests explicitly declare/stub non-owned consumers.",
      "prevention_rule": "For FAILED CI, always fetch actual failed job logs when the connector exposes them. For partial/mock authority fixtures, identify every consumer introduced by the change and either construct complete authority or explicitly isolate unrelated consumers; never weaken production validation to satisfy a narrow unit test.",
      "symptom": "PR #556 remained red across repeated runs because orchestration stopped at workflow/job failure summaries instead of retrieving the available job logs; once logs were inspected, three reviewed-export routing tests were shown to use intentionally partial promotion-marker fixtures that the new EOF note-gap validator correctly rejected as invalid production authority.",
      "fix": "Retrieve failed job logs before any opaque-blocker classification. Keep production fail-closed behavior intact. In tests intentionally using partial authority fixtures, explicitly stub unrelated authority consumers so the test remains scoped to its owned behavior."
    },
    {
      "id": "branch-write-target-safety-2026-09-09",
      "status": "corrected_during_run",
      "root_cause": "Feature-file creation was attempted before the intended feature branch had been successfully created, causing disposable placeholder files to be written to the default branch. Repeated write attempts compounded commit-history noise even though each placeholder was immediately removed and no product logic remained on main.",
      "regression_protection": [
        "engineering/regression-memory.json"
      ],
      "verification": "Every accidental placeholder file was deleted from main before feature implementation continued. The real feature branch was then created explicitly with the branch-creation operation, and subsequent feature writes targeted only orchestrator/eof-beat-phase-solver.",
      "residual_risk": "The add/delete cleanup commits remain as harmless default-branch history noise because rewriting main would be destructive and is not justified.",
      "prevention_rule": "Before any feature file create/update/delete, first create or resolve the intended non-default branch and verify that branch creation succeeded. Never use a contents write as a branch-existence probe, and never fall back to the default branch when a requested feature branch is missing.",
      "symptom": "Temporary placeholder files briefly appeared on main while starting the #455 beat-phase implementation, despite the change being intended for a gated feature branch.",
      "fix": "Removed each placeholder immediately, stopped feature writes, created orchestrator/eof-beat-phase-solver explicitly from main, and resumed implementation only after branch creation succeeded."
    },
    {
      "id": "promote-preflight-green-missing-gh-repo-context-2026-09-11",
      "status": "fix_landed_awaiting_live_promotion_proof",
      "root_cause": "The verify-promotion-gate job in .github/workflows/promote-preflight-green.yml intentionally has no actions/checkout step, but its gh run download / gh pr list / gh pr view / gh pr ready invocations had no --repo flag and no GH_REPO env var, so the gh CLI tried to infer the repository from a local git clone that did not exist and failed with 'fatal: not a git repository' before contacting the GitHub API.",
      "regression_protection": [
        "tests/test_promote_preflight_green_workflow.py",
        ".github/workflows/promote-preflight-green.yml"
      ],
      "verification": "tests/test_promote_preflight_green_workflow.py passes against the fixed workflow and was confirmed (via git stash of only the workflow file) to fail with the exact pre-fix symptom against the unpatched file. Live GitHub history showed this job failing on every orchestrator/*- and agent/*-branch Preflight success from run 160 through run 174 (2026-09-10T13:32Z through 2026-09-11T03:31Z).",
      "residual_risk": "No currently open PR depends on this gate, so the fix has not yet been exercised by a real promotion; the next orchestrator/*/agent/* Preflight success is the first live proof.",
      "prevention_rule": "Before trusting a gh-CLI-driven workflow job that intentionally has no actions/checkout, verify every gh invocation in that job has explicit repository context (GH_REPO env or --repo); absence of checkout and absence of repo context are two different design choices and only the second is a defect.",
      "symptom": "Verify Preflight-Green Branch reported conclusion failure on every orchestrator/*- and agent/*-branch Preflight success, so draft PRs on those branches were never auto-promoted to ready-for-review by this gate.",
      "fix": "Added env: GH_REPO: ${{ github.repository }} at the verify-promotion-gate job level so every gh invocation in the job (download, list, view, ready) has explicit repository context without adding a checkout step."
    },
    {
      "id": "github-token-created-pr-action-required-2026-09-11",
      "status": "root_cause_confirmed_external_configuration_fix_pending",
      "root_cause": "The Reconcile Live Status workflow (.github/workflows/reconcile-live-status.yml) pushes the automation/status-reconcile branch and creates/updates its pull request using the repository GITHUB_TOKEN (github.token). GitHub intentionally places pull_request workflow runs caused by a GITHUB_TOKEN-authored push/PR into an approval-required state, so every required check completes as action_required with zero jobs ever instantiated. This is an automation identity/credential policy interaction, not a Rocksmith product-code or test failure.",
      "regression_protection": [
        "engineering/regression-memory.json",
        "docs/engineering/error-resolution-ledger.md",
        ".github/workflows/reconcile-live-status.yml"
      ],
      "verification": "Reproduced live on PR #575 (branch automation/status-reconcile), head 7f6cbf49b7351c37572bb83046ccb872d07f7e9d: all six required workflow runs created at 2026-09-11T02:53:13Z (Golden Verification run 34556335385, Execution Director 34556335391, CI 34556335402, Security - TruffleHog 34556335414, Control Plane 34556335420, Windows Desktop 34556335428) completed with conclusion action_required, matching the pattern first confirmed on PR #565's head c730d67ec8ae83455c20fb71367fa3884364b46e. The same branch's prior head (also c730d67e) reached conclusion success only after triggering_actor jweter manually ran/approved those workflows at 2026-09-11T01:00-02:07Z; a subsequent force-push to 7f6cbf49 reset the PR back into the unapproved action_required state, directly proving the block recurs on every push rather than being a one-time fluke.",
      "residual_risk": "Until reconciliation PR creation/push uses a non-GITHUB_TOKEN trusted identity, every newly created or updated automation/status-reconcile PR will recur in action_required with zero jobs and consume triage time or require a human to manually approve workflow runs on each push.",
      "prevention_rule": "When all pull_request workflows on a github-actions[bot]-created/updated PR return action_required with zero jobs, classify AUTOMATION_IDENTITY/GITHUB_ACTIONS_APPROVAL_POLICY before attempting code repair; do not repeatedly rerun or inspect nonexistent job logs. For unattended CI on workflow-created PRs, the durable fix is to push/create with a GitHub App installation token or scoped personal access token stored as a repository secret instead of github.token -- this requires a credential the repository owner must provision, so it is not applied automatically. A prior attempt to record this finding (PR #573) was closed by the repository owner because its ledger edit could not be safely reconciled without risking history replacement; edits to docs/engineering/error-resolution-ledger.md must be strictly additive (append one new dated entry) and must never regenerate or replace the file.",
      "symptom": "PR #565 and, again on 2026-09-11, PR #575 each repeatedly show every required workflow as action_required with no jobs or logs, making automation/status-reconcile look like it is failing CI even though no workflow job ever executed.",
      "fix": "Immediate one-off recovery: a repository user with write access selects Approve workflows to run on the affected PR. Durable fix (not yet applied, needs a provisioned credential): change reconcile-live-status.yml so the git push and gh pr create/update operations authenticate with a dedicated GitHub App installation token or fine-grained personal access token stored as a repository Actions secret, rather than github.token."
    }
  ]
}
