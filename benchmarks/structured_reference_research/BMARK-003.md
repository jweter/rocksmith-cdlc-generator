# BMARK-003 research note

BMARK-003 is Killswitch Engage — Rose of Sharyn. The committed YAML record captures public Songsterr page metadata only and is classified as `other_structured_notation` because the public page does not identify Guitar Pro format.

This is discovery metadata, not source acceptance. No notation, audio, local paths, private exports, or source bytes are committed. Benchmark promotion still requires the separate provenance and explicit human-review gates.
