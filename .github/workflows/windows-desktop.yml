name: Windows Desktop

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - "agent/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-desktop:
    name: Windows desktop app
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install desktop runtime and packager
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[beat,guitarpro,audio]"
          python -m pip install "pyinstaller>=6.10,<7"

      - name: Verify desktop modules import
        shell: pwsh
        run: |
          python -c "import tkinter; import sounddevice; import rocksmith_cdlc_generator.desktop_shell; import rocksmith_cdlc_generator.song_workspace_playback_ui; import rocksmith_cdlc_generator.waveform_cache; print('desktop playback imports OK')"

      - name: Build Windows application
        shell: pwsh
        run: |
          python -m PyInstaller `
            --noconfirm `
            --clean `
            --windowed `
            --onedir `
            --name RocksmithCDLCGenerator `
            --collect-submodules rocksmith_cdlc_generator `
            --collect-all librosa `
            --collect-all guitarpro `
            --collect-all sounddevice `
            scripts/windows_desktop_entry.py

      - name: Verify application bundle
        shell: pwsh
        run: |
          if (-not (Test-Path "dist/RocksmithCDLCGenerator/RocksmithCDLCGenerator.exe")) {
            throw "Desktop executable was not produced"
          }
          Get-ChildItem "dist/RocksmithCDLCGenerator" | Select-Object Name, Length

      - name: Upload Windows desktop artifact
        uses: actions/upload-artifact@v4
        with:
          name: RocksmithCDLCGenerator-Windows
          path: dist/RocksmithCDLCGenerator/
          if-no-files-found: error
          retention-days: 14
